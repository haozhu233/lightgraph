% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lightgraph.R
\name{lightgraph}
\alias{lightgraph}
\title{Create a lightgraph network visualization}
\usage{
lightgraph(
  nodes = NULL,
  edges,
  node_groups = NULL,
  remove_unconnected = FALSE,
  node_metric = NULL,
  metric_map = "size",
  metric_size_range = c(4, 20),
  metric_colors = c("#c6dbef", "#08306b"),
  metric_label = NULL,
  show_arrows = FALSE,
  show_labels = TRUE,
  show_ellipses = TRUE,
  show_legend = TRUE,
  show_statistics = FALSE,
  show_tooltips = TRUE,
  highlight_neighbors = TRUE,
  neighbor_fade = 0.15,
  ego_filter = TRUE,
  ego_depth = 1,
  layout = "force",
  simulation_strength = 4000,
  link_distance = 100,
  group_attraction = 0.3,
  warmup_ticks = "auto",
  node_size = 7,
  node_color = NULL,
  label_font_size = 5,
  edge_width = NULL,
  edge_color = NULL,
  edge_opacity = NULL,
  edge_weight_to_width = FALSE,
  edge_weight_to_opacity = FALSE,
  weight_width_range = c(0.5, 4),
  weight_opacity_range = c(0.05, 0.6),
  theme = "light",
  background_color = NULL,
  auto_fit = TRUE,
  zoom_range = c(0.1, 5),
  pixel_ratio = NULL,
  export_scale = 2,
  config = NULL,
  width = NULL,
  height = "800px",
  elementId = NULL
)
}
\arguments{
\item{nodes}{A data frame with node information. Must contain an 'id' column.
Optional columns: 'group' for node grouping, 'color' for custom colors,
'size' for node sizes. May be omitted (NULL) when \code{edges} is given;
nodes are then derived from the edge list.}

\item{edges}{A data frame with edge information. Must contain 'source' and 'target'
columns. Optional 'weight' column for edge weights.}

\item{node_groups}{An optional named vector mapping node ids to group
labels, or the string "auto" to detect communities automatically via
\code{\link{lg_communities}}. Values given here win over an existing
'group' column in \code{nodes}.}

\item{remove_unconnected}{Logical, whether to remove nodes with no
connections (default: FALSE; the Python binding defaults to TRUE
because adjacency-matrix workflows often carry empty rows).}

\item{node_metric}{Optional named numeric vector (names = node ids) mapped
to node size and/or color per metric_map — see the lg_* analytics
functions, e.g. \code{\link{lg_pagerank}}. Explicit size/color columns
in nodes take precedence.}

\item{metric_map}{Character, which channel node_metric drives: "size",
"color", or "both" (default: "size").}

\item{metric_size_range}{Numeric length-2, node size range for
metric-driven sizing (default: c(4, 20)).}

\item{metric_colors}{Character length-2, low/high hex colors for
metric-driven coloring (default: c("#c6dbef", "#08306b")).}

\item{metric_label}{Character, title for the metric section of the legend
(e.g. "PageRank"; default: "Metric"). Shown whenever node_metric drives
size and/or color and show_legend is TRUE.}

\item{show_arrows}{Logical, whether to show directional arrows on edges (default: FALSE).}

\item{show_labels}{Logical, whether to show node labels (default: TRUE).}

\item{show_ellipses}{Logical, whether to show group ellipses (default: TRUE).}

\item{show_legend}{Logical, whether to show the group legend (default: TRUE).}

\item{show_statistics}{Logical, whether to show the statistics panel (default: FALSE).}

\item{show_tooltips}{Logical, whether to show tooltips on hover (default: TRUE).}

\item{highlight_neighbors}{Logical, fade everything outside the 1-hop
neighborhood of hovered/selected nodes (default: TRUE).}

\item{neighbor_fade}{Numeric, opacity multiplier applied to faded elements
while highlighting (default: 0.15).}

\item{ego_filter}{Logical, double-clicking a node shows only its k-hop
neighborhood; double-click empty space or press Escape to restore
(default: TRUE).}

\item{ego_depth}{Integer, neighborhood depth in hops for the ego filter
(default: 1).}

\item{layout}{Character, layout algorithm: "force" or "circular" (default: "force").}

\item{simulation_strength}{Numeric, repulsion strength for force layout (default: 4000).}

\item{link_distance}{Numeric, target distance between connected nodes (default: 100).}

\item{group_attraction}{Numeric, strength of the pull toward each group's
centroid in the force layout (default: 0.3; 0 disables).}

\item{warmup_ticks}{"auto" or integer. Synchronous layout ticks run before
the first paint so the graph appears already untangled. "auto" spends a
roughly fixed time budget; a number forces that tick count; 0 disables.}

\item{node_size}{Numeric, default size for nodes (default: 7).}

\item{node_color}{Character, default node fill color (hex). Defaults to
the theme color.}

\item{label_font_size}{Numeric, font size for labels (default: 5).}

\item{edge_width}{Numeric, base width for edges (JS default 0.45).}

\item{edge_color}{Character, default edge color (hex). Defaults to the
theme color.}

\item{edge_opacity}{Numeric, opacity for edges 0-1. By default opacity
adapts automatically to the on-screen edge density and zoom level;
setting a value pins it.}

\item{edge_weight_to_width}{Logical, scale edge width by weight (default: FALSE).}

\item{edge_weight_to_opacity}{Logical, scale edge opacity by weight (default: FALSE).}

\item{weight_width_range}{Numeric length-2, edge width range used by
edge_weight_to_width (default: c(0.5, 4)).}

\item{weight_opacity_range}{Numeric length-2, edge opacity range used by
edge_weight_to_opacity (default: c(0.05, 0.6)).}

\item{theme}{Character, UI/canvas color theme: "light" or "dark"
(default: "light").}

\item{background_color}{Character, background color hex code. Defaults to
white in the light theme and near-black in the dark theme.}

\item{auto_fit}{Logical, zoom to fit the graph once the layout settles;
skipped if the user already panned/zoomed manually (default: TRUE).}

\item{zoom_range}{Numeric length-2, minimum and maximum zoom factors
(default: c(0.1, 5)).}

\item{pixel_ratio}{Numeric, backing-store resolution multiplier. Defaults
to the display's devicePixelRatio (sharp on retina); set 1 to trade
sharpness for speed on very large graphs.}

\item{export_scale}{Numeric, resolution multiplier for PNG export,
relative to on-screen size (default: 2).}

\item{config}{Optional named list of raw lightGraph config (JS shape,
e.g. \code{list(nodes = list(borderWidth = 2))}) deep-merged over
everything above — full access to any JS option without a dedicated
argument.}

\item{width}{Width of the visualization (optional, defaults to automatic sizing).}

\item{height}{Height of the visualization (optional, defaults to 800px).}

\item{elementId}{An optional element ID for the widget.}
}
\value{
An htmlwidget object that can be displayed in RStudio Viewer, R Markdown,
  or Shiny applications.
}
\description{
This function creates an interactive network visualization using the lightGraph
JavaScript library via htmlwidgets. Its parameters mirror the Python
binding's \code{net_vis()} one-to-one, and expose the full lightGraph JS
configuration.
}
\examples{
\dontrun{
# Create sample network data
nodes <- data.frame(
  id = c("A", "B", "C", "D"),
  group = c("1", "1", "2", "2")
)
edges <- data.frame(
  source = c("A", "B", "C"),
  target = c("B", "C", "D"),
  weight = c(1, 1, 1)
)

# Create visualization with default settings
lightgraph(nodes, edges)

# Nodes can be derived from the edge list, with communities detected
# and PageRank mapped to node size:
lightgraph(
  edges = edges,
  node_groups = "auto",
  node_metric = lg_pagerank(edges)
)

# Create visualization with custom settings
lightgraph(nodes, edges,
  layout = "circular",
  show_statistics = TRUE,
  background_color = "#f5f5f5"
)
}

}
